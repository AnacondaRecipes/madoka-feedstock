{% set name = "madoka" %}
{% set version = "0.7.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: e258baa84fc0a3764365993b8bf5e1b065383a6ca8c9f862fb3e3e709843fae7

build:
  number: 0
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  build:
    - {{ stdlib('c') }}
    - {{ compiler('cxx') }}
  host:
    - pip
    - python
    - setuptools
    - wheel
  run:
    - python

test:
  imports:
    - madoka
  commands:
    - pip check
  requires:
    - pip
    - python

about:
  home: https://github.com/ikegami-yukino/madoka-python
  license: BSD-3-Clause
  license_family: BSD
  license_file: LICENSE
  summary: Memory-efficient CountMin Sketch key-value structure (based on Madoka C++ library)
  description: Memory-efficient CountMin Sketch key-value structure (based on Madoka C++ library)
  doc_url: https://github.com/ikegami-yukino/madoka-python
  dev_url: https://github.com/ikegami-yukino/madoka-python

extra:
  recipe-maintainers:
    - jan-janssen
